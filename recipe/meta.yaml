{% set name = "posix_ipc" %}
{% set version = "1.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/posix_ipc-{{ version }}.tar.gz
  sha256: 6e559ac5bb5f6f233c396103f4868e383bbd8f4e54d20876910896f47d353448

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - python
    - pip
    - setuptools
  run:
    - python

test:
  imports:
    - posix_ipc
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/osvenskan/posix_ipc
  summary: POSIX IPC primitives (semaphores, shared memory and message queues) for Python
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - akrherz
